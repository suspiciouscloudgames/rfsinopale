# Runtime binary assets

브라우저가 `/assets/...` URL로 직접 읽는 이미지, 모델, 오디오, 비디오 영역이다.

새 콘텐츠는 `images/`, `depth-portal/`, `models/`, `audio/`, `video/`의 표준 하위 폴더에 추가한다. 기존 `hub-background.mp4`와 `archive-signal.mp4`는 현재 코드 호환을 위해 루트에 유지한다.

상세 규칙은 `docs/content_asset_hierarchy_and_workflow.md`를 참조한다.
